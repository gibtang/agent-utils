# QA Test Cases — Agent Utils (agent-utils.com)

> Auto-generated by Sazabi (QA Engineer) from GSC property scan + repo structure analysis.
> Last updated: 2026-06-20

## P0 — Critical User Flows (API-first product)

### TC-AU-001: Image Upload / File Host — Upload
- **URL:** POST https://www.agent-utils.com/v1/upload
- **Steps:** POST multipart with image file → verify response
- **Expected:** 200 with {id, url} — file stored and accessible
- **Edge:** Empty file → 400. Oversized file → 413. Non-image → error
- **Note:** Upload action is `/v1/upload` (v2 agent-key auth); the returned hosted `url` is the public `/api/file-host/{id}` read endpoint (capability token, no auth). Verify both.

### TC-AU-002: Image Upload / File Host — Retrieve
- **URL:** GET https://www.agent-utils.com/api/file-host/{id}
- **Steps:** Upload file → GET by returned ID → verify file content matches
- **Expected:** 200 with file content or redirect to stored URL
- **Edge:** Invalid ID → 404. Expired file → 404 or 410

### TC-AU-003: Dead Letter Queue — Create entry
- **URL:** POST https://www.agent-utils.com/api/dlq
- **Steps:** POST DLQ entry with message payload → verify created
- **Expected:** 201 with {id, status, payload, created_at}
- **Edge:** Empty payload → 400. Very large payload → size limit

### TC-AU-004: Dead Letter Queue — List and retry
- **URL:** GET https://www.agent-utils.com/api/dlq, POST https://www.agent-utils.com/api/dlq/{id}/retry
- **Steps:** List DLQ entries → retry one → verify status changes
- **Expected:** List returns paginated results, retry moves status to "processing" or "retried"

### TC-AU-005: Human-in-the-Loop Checkpoint — Create + Resume
- **URL:** POST https://www.agent-utils.com/api/checkpoint, POST https://www.agent-utils.com/api/checkpoint/{id}/resume
- **Steps:** Create checkpoint → verify it blocks → resume with approved token → verify released
- **Expected:** Checkpoint creates pending state, resume transitions to approved
- **Edge:** Resume without valid token → 403. Resume non-pending checkpoint → 400

### TC-AU-006: Checkpoint public token flow
- **URL:** GET https://www.agent-utils.com/api/checkpoint/public/{token}
- **Steps:** Create checkpoint → get public token → access via public URL → approve
- **Expected:** Public token grants view + approve access without auth

### TC-AU-007: PII Shield — Scan and clean
- **URL:** POST https://www.agent-utils.com/api/shield, POST https://www.agent-utils.com/api/shield/clean
- **Steps:** Send text with PII (email, phone, SSN) → scan → clean → verify PII removed
- **Expected:** Scan returns detected PII entities, clean returns redacted text

### TC-AU-008: OTP — Generate and verify
- **URL:** POST https://www.agent-utils.com/api/otp, GET https://www.agent-utils.com/api/otp/{id}
- **Steps:** Generate OTP → verify code → check status
- **Expected:** OTP generated with expiry, verify endpoint confirms code match
- **Edge:** Wrong code → 400. Expired OTP → 410

### TC-AU-009: Notification Router — Send
- **URL:** POST https://www.agent-utils.com/api/notify
- **Steps:** POST notification with channel + message → verify delivery
- **Expected:** 200/202 with delivery confirmation
- **Edge:** Invalid channel → 400. Missing message → 400

### TC-AU-010: KV Store — Set and Get
- **URL:** POST https://www.agent-utils.com/api/kv, GET https://www.agent-utils.com/api/kv/{key}
- **Steps:** Set key-value pair → GET by key → verify value matches
- **Expected:** 200 on both, value persists
- **Edge:** Non-existent key → 404. Empty value → 400

### TC-AU-011: Rate Limiter — Check and reset
- **URL:** POST https://www.agent-utils.com/api/rate-limit/check, POST https://www.agent-utils.com/api/rate-limit/reset
- **Steps:** Check rate limit → exceed threshold → verify blocked → reset → verify unblocked
- **Expected:** Returns {allowed, remaining, limit}. After limit → allowed=false. After reset → allowed=true

### TC-AU-012: Webhook Inbox — Receive
- **URL:** POST https://www.agent-utils.com/api/webhook, GET https://www.agent-utils.com/api/webhook/{id}
- **Steps:** POST webhook payload → GET received webhook by ID
- **Expected:** Webhook stored, retrievable with full headers + body

### TC-AU-013: Audit Log — Create and list
- **URL:** POST https://www.agent-utils.com/api/audit, GET https://www.agent-utils.com/api/audit
- **Steps:** Create audit entry → list → verify entry appears
- **Expected:** Audit trail with timestamp, actor, action

### TC-AU-014: Agent Form — Create and retrieve
- **URL:** POST https://www.agent-utils.com/api/form, GET https://www.agent-utils.com/api/form/{id}
- **Steps:** Create form submission → GET by ID
- **Expected:** Form data stored with schema, retrievable

## P1 — Documentation & Spec

### TC-AU-015: Public docs pages render
- **URLs:** /docs/image-upload, /docs/dlq, /docs/checkpoint
- **Steps:** Visit each docs page → verify content renders
- **Expected:** Documentation with API examples, code snippets

### TC-AU-016: OpenAPI spec validity
- **URL:** GET https://www.agent-utils.com/openapi.json
- **Steps:** Fetch OpenAPI spec → validate all listed endpoints exist as route files
- **Expected:** Every endpoint in spec has a corresponding route.ts file
- **Known Issue:** 8 phantom endpoints may exist in spec without route files (previously identified)

### TC-AU-017: API endpoint consistency
- **Steps:** Confirm `/v1/upload` (authed write) returns a `/api/file-host/{id}` url (public read)
- **Expected:** Consistent endpoint naming, no dead routes

## P2 — Edge Cases

### TC-AU-018: Auth and rate limiting on all endpoints
- **Steps:** Test each API endpoint without auth header → verify behavior
- **Expected:** Consistent auth enforcement (API key required)

### TC-AU-019: CORS headers
- **Steps:** Send OPTIONS preflight to each endpoint
- **Expected:** Proper CORS headers returned for browser-based calls

### TC-AU-020: Input validation — all POST endpoints
- **Steps:** Send malformed JSON, missing required fields, oversized payloads to each POST endpoint
- **Expected:** 400 with descriptive error messages

### TC-AU-021: Mobile docs readability
- **Steps:** View docs pages at 375px width
- **Expected:** Code blocks scroll horizontally, text wraps, no layout breaks

### TC-AU-022: Homepage renders
- **URL:** https://www.agent-utils.com/
- **Steps:** Load homepage → verify all 11 features listed
- **Expected:** Clean landing page with feature descriptions

### TC-AU-023: Shield hydrate endpoint
- **URL:** POST https://www.agent-utils.com/api/shield/hydrate
- **Steps:** Send redacted text → hydrate with original values → verify restoration
- **Expected:** Redacted tokens replaced with original values

### TC-AU-024: DLQ delete entry
- **URL:** DELETE https://www.agent-utils.com/api/dlq/{id}
- **Steps:** Create DLQ entry → delete it → verify removed from list
- **Expected:** 200/204 on delete, entry no longer in list

### TC-AU-025: Checkpoint delete
- **URL:** DELETE https://www.agent-utils.com/api/checkpoint/{id}
- **Steps:** Create checkpoint → delete → verify gone
- **Expected:** 200/204, checkpoint removed

## Known Issues Flagged
- `/v1/upload` (authed write) vs `/api/file-host/{id}` (public read) — distinct by design, not an inconsistency
- 8 phantom endpoints in OpenAPI spec without route files (previously identified)
- Only 3 of 11 tools have public docs pages (image-upload, dlq, checkpoint)
- Deployment lag previously identified — verify all endpoints return 200 on production
