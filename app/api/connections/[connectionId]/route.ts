import { NextRequest, NextResponse } from 'next/server';
import { requireOwner } from '@/lib/owner/auth'; import { revokeConnection } from '@/lib/connections/service'; import { resourceId } from '@/lib/core/ids'; import { DomainError, Errors } from '@/lib/core/errors'; import { failure, success } from '@/lib/core/envelope'; import { consume, RATE_LIMIT_POLICIES, rateLimitHeaders } from '@/lib/core/rate-limit';
export async function DELETE(request: NextRequest, { params }: { params: Promise<{ connectionId: string }> }) { const requestId = resourceId('req_'); try { const actor = await requireOwner(request); const rate = await consume({ scope: 'owner', identifier: actor.accountId }, RATE_LIMIT_POLICIES.OWNER.limit, RATE_LIMIT_POLICIES.OWNER.windowSeconds); if (!rate.allowed) throw Errors.planLimitReached(); const { connectionId } = await params; await revokeConnection(actor, connectionId); const headers = rateLimitHeaders(rate); return NextResponse.json(success({ revoked: true }, requestId, { headers }).body, { headers }); } catch (error) { const e = error instanceof DomainError ? error : Errors.temporarilyUnavailable(); return NextResponse.json(failure(e, requestId).body, { status: e.http }); } }
