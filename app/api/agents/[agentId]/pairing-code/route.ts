import { NextRequest, NextResponse } from 'next/server';
import { requireOwner } from '@/lib/owner/auth'; import { createPairingCode } from '@/lib/connections/service'; import { resourceId } from '@/lib/core/ids'; import { DomainError, Errors } from '@/lib/core/errors'; import { failure, success } from '@/lib/core/envelope'; import { consume, RATE_LIMIT_POLICIES, rateLimitHeaders } from '@/lib/core/rate-limit';
export async function POST(request: NextRequest, { params }: { params: Promise<{ agentId: string }> }) { const requestId = resourceId('req_'); try { const actor = await requireOwner(request); const rate = await consume({ scope: 'owner', identifier: actor.accountId }, RATE_LIMIT_POLICIES.OWNER.limit, RATE_LIMIT_POLICIES.OWNER.windowSeconds); if (!rate.allowed) throw Errors.planLimitReached(); const { agentId } = await params; const data = await createPairingCode(actor, agentId, await request.json()); const headers = rateLimitHeaders(rate); return NextResponse.json(success(data, requestId, { status: 201, headers }).body, { status: 201, headers }); } catch (error) { const e = error instanceof DomainError ? error : Errors.temporarilyUnavailable(); return NextResponse.json(failure(e, requestId).body, { status: e.http }); } }
