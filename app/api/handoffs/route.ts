import { NextRequest, NextResponse } from 'next/server';
import { requireOwner } from '@/lib/owner/auth'; import { listHandoffs } from '@/lib/handoff/service'; import { resourceId } from '@/lib/core/ids'; import { DomainError, Errors } from '@/lib/core/errors'; import { failure, success } from '@/lib/core/envelope'; import { consume, RATE_LIMIT_POLICIES, rateLimitHeaders } from '@/lib/core/rate-limit';
const serialize = (row: Awaited<ReturnType<typeof listHandoffs>>[number]) => ({ ...row, createdAt: row.createdAt.toISOString(), lastRetrievedAt: row.lastRetrievedAt?.toISOString() ?? null, linkExpiresAt: row.linkExpiresAt.toISOString(), sessionExpiresAt: row.sessionExpiresAt.toISOString() });
export async function GET(request: NextRequest) { const requestId = resourceId('req_'); try { const actor = await requireOwner(request); const rate = await consume({ scope: 'owner', identifier: actor.accountId }, RATE_LIMIT_POLICIES.OWNER.limit, RATE_LIMIT_POLICIES.OWNER.windowSeconds); if (!rate.allowed) throw Errors.planLimitReached(); const data = (await listHandoffs(actor)).map(serialize); const headers = rateLimitHeaders(rate); return NextResponse.json(success(data, requestId, { headers }).body, { headers }); } catch (error) { const e = error instanceof DomainError ? error : Errors.temporarilyUnavailable(); return NextResponse.json(failure(e, requestId).body, { status: e.http }); } }
