import { NextRequest, NextResponse } from 'next/server';
import { requireConnection } from '@/lib/connections/auth'; import { getOperation } from '@/lib/contracts/registry'; import '@/lib/handoff/operations'; import { resourceId } from '@/lib/core/ids'; import { DomainError, Errors } from '@/lib/core/errors'; import { failure, success } from '@/lib/core/envelope'; import { consume, RATE_LIMIT_POLICIES, rateLimitHeaders } from '@/lib/core/rate-limit';
async function actor(request: NextRequest) { const value = await requireConnection(request); const [one, account] = await Promise.all([consume({ scope: 'connection', identifier: value.connectionId }, RATE_LIMIT_POLICIES.CONNECTION.limit, RATE_LIMIT_POLICIES.CONNECTION.windowSeconds), consume({ scope: 'connection_account', identifier: value.accountId }, RATE_LIMIT_POLICIES.CONNECTION_PER_ACCOUNT.limit, RATE_LIMIT_POLICIES.CONNECTION_PER_ACCOUNT.windowSeconds)]); if (!one.allowed || !account.allowed) throw Errors.planLimitReached(); return { value, headers: rateLimitHeaders({ remaining: Math.min(one.remaining, account.remaining), retryAfterSeconds: Math.max(one.retryAfterSeconds, account.retryAfterSeconds) }) }; }
export async function POST(request: NextRequest) { const requestId = resourceId('req_'); try { const { value, headers } = await actor(request); const op = getOperation('handoff_close')!; const parsed = op.input.safeParse(await request.json()); if (!parsed.success) throw Errors.validationFailed(); const data = await op.execute({ actor: value, requestId }, parsed.data); return NextResponse.json(success(data, requestId, { headers }).body, { headers }); } catch (error) { const e = error instanceof DomainError ? error : Errors.temporarilyUnavailable(); return NextResponse.json(failure(e, requestId).body, { status: e.http }); } }
