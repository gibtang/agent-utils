import { NextRequest, NextResponse } from 'next/server'; import { requireConnection } from '@/lib/connections/auth'; import { getConnectionStatus } from '@/lib/connections/service'; import { resourceId } from '@/lib/core/ids'; import { DomainError, Errors } from '@/lib/core/errors'; import { failure, success } from '@/lib/core/envelope'; import { consume, RATE_LIMIT_POLICIES, rateLimitHeaders } from '@/lib/core/rate-limit';
export async function GET(request: NextRequest) { const requestId = resourceId('req_'); try { const actor = await requireConnection(request); const [connectionRate, accountRate] = await Promise.all([consume({ scope: 'connection', identifier: actor.connectionId }, RATE_LIMIT_POLICIES.CONNECTION.limit, RATE_LIMIT_POLICIES.CONNECTION.windowSeconds), consume({ scope: 'connection_account', identifier: actor.accountId }, RATE_LIMIT_POLICIES.CONNECTION_PER_ACCOUNT.limit, RATE_LIMIT_POLICIES.CONNECTION_PER_ACCOUNT.windowSeconds)]); if (!connectionRate.allowed || !accountRate.allowed) throw Errors.planLimitReached(); const data = await getConnectionStatus(actor); const headers = rateLimitHeaders({ remaining: Math.min(connectionRate.remaining, accountRate.remaining), retryAfterSeconds: Math.max(connectionRate.retryAfterSeconds, accountRate.retryAfterSeconds) }); return NextResponse.json(success(data, requestId, { headers }).body, { headers }); } catch (error) { const e = error instanceof DomainError ? error : Errors.temporarilyUnavailable(); return NextResponse.json(failure(e, requestId).body, { status: e.http }); } }
