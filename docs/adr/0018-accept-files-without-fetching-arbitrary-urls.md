---
status: accepted
---

# Accept Files without fetching arbitrary URLs

At launch, Files enter AgentUtils through an upload initiated by an authorised Agent, a short-lived upload link created for a person or external service, or content included directly in an Inbox request. AgentUtils does not fetch arbitrary URLs supplied by an Agent. The Agent normally manages the transfer while the Agent Owner sees progress, access, expiry and recovery controls in the dashboard.
