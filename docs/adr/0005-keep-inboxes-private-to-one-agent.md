---
status: accepted
---

# Keep Inboxes private to one Agent by default

Each Inbox is assigned to one Agent and is visible only to that Agent and its Agent Owner by default. An Inbox created by an Agent is assigned automatically; the Agent Owner may later share or reassign it without changing its receiving address. This privacy boundary prevents unrelated agents from reading sensitive Events or accidentally processing the same Event while preserving runtime-independent handoff.
