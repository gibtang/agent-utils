---
status: accepted
---

# Launch State with versioned JSON records

The initial State contract consists of named JSON State Records that can be stored, retrieved, listed, deleted and optionally expired. Every State Record has a version and supports compare-and-set updates; locks, counters, JSON Patch, subscriptions, full history and semantic memory are excluded until demonstrated workflows require them.
